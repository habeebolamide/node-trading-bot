# Archive

Completed change folders, moved here from `../changes/` when done, named
`<YYYY-MM-DD>-<change-name>/` with a completion summary at the top of `proposal.md`. This is
load-bearing project history — the permanent record of what was built, when, per which plan
section. Not clutter; do not delete.
