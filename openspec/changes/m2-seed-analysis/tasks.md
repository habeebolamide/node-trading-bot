# Tasks: m2-seed-analysis

`[x]` done · `[~]` built but not yet run (blocked on Helius connection)

## 0. Input
- [x] roster of 110 wallet addresses written to `scripts/seed/wallets.txt` (gitignored)

## 1. Seed run
- [x] `scripts/src/seed-wallets.ts` — backfill + reconstruct + score every roster wallet
      (reuses m2 changes 1 & 2; `scoreAllWallets` called without a bus → no Redis); `seed-wallets` script
- [~] RUN it — BLOCKED: Helius webhook/key path not connected yet (see setup steps in chat)

## 2. Analysis pass
- [x] `analysis/co-buy.ts` — session-gap co-buy clustering (analysis-only; NOT M3 funder clustering)
- [x] `analysis/seed-metrics.ts` — analyzeBatchingWindow / analyzeProfitLadder / analyzeWalletExit
      / analyzeFreshness + quantile (pure)
- [x] `scripts/src/seed-analysis.ts` — loads local Postgres → runs analyses → writes the doc
- [~] RUN it — BLOCKED: needs the seed run's data first

## 3. Output
- [~] `docs/research/seed-history-analysis.md` — generated by the runner once data exists (not yet)
- [ ] record recommended values for the 4 tunables (produced by the run; wire into config at M4+)

## 4. Tests
- [x] unit: co-buy clustering + quantile + batching-window + profit-ladder + freshness (7 tests, pure)

## 5. Wrap-up
- [~] typecheck green; 102/105 tests pass. NOT archived — the seed run + analysis have not been
      executed end-to-end (Helius not connected). Archive after a real run produces the doc.
