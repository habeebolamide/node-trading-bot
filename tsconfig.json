{
  "//": "Solution-style root config. References are added as each workspace is created (OpenSpec change by change). `tsc --build` walks these in dependency order.",
  "files": [],
  "references": [
    { "path": "packages/domain" },
    { "path": "packages/events" },
    { "path": "packages/database" },
    { "path": "apps/api" },
    { "path": "apps/worker" }
  ]
}
